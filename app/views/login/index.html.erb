
<% puts "session id is #{session[:user_id]}"%>


<% if session[:user_id]%>

<%= link_to "Logout", url_for(controller: :login, action: :logout), method: :post%>

<%else%>
<%= link_to "Login", url_for(controller: :login, action: :vlogin), method: :get%>
<%end%>